<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
     
      <%= render 'form', f: f %>
      <%= f.submit "Create Account", class: "brt btn-primary" %>
    <% end %>  
  </div>
</div>
